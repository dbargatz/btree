#!/bin/bash

# Installs the tools, libraries, and dependencies needed to build and test 
# btree. This was hand-written, not generated with autoconf/autotools.

# Upgrade pip3 if necessary, then install/upgrade Meson and Ninja to the latest
# versions available.
python3 -m pip install --user --upgrade pip
python3 -m pip install --user --upgrade meson ninja
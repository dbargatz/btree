#!/bin/bash

# Installs the tools, libraries, and dependencies needed to build and test 
# btree. This was hand-written, not generated with autoconf/autotools.

# Upgrade pip if necessary, then install/upgrade Meson and Ninja to the latest
# versions available.
pip install --user --upgrade pip
pip install --user --upgrade meson ninja